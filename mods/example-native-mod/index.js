export function onActivate() {
    console.log('[example-native-mod] activate fired — Phase 1.5 native tier is live');
}

export function onDisable() {
    console.log('[example-native-mod] disable fired — Phase 1.5 teardown');
}